-- ============================================================
-- إضافة حقل الملاحظات لجدول التقييمات
-- شغّل هذا فقط (بدون إعادة تشغيل schema.sql كاملاً)
-- ============================================================

alter table evaluations add column if not exists notes text;
